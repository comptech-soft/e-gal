const root = 'superadmin/roles/'

module.exports = {
    insert: {
        endpoint: root + 'insert'
    },
    update: {
        endpoint: root + 'update'
    },
    delete: {
        endpoint: root + 'superadmin/roles/delete'
    }
}