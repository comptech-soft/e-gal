const root = 'superadmin/locations/'

module.exports = {
    insert: {
        endpoint: root + 'insert'
    },
    update: {
        endpoint: root + 'update'
    },
    delete: {
        endpoint: root + 'delete'
    }
}