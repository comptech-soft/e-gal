const root = 'superadmin/users/'

module.exports = {
    insert: {
        endpoint: root + 'insert'
    },
    update: {
        endpoint: root + 'update'
    },
    delete: {
        endpoint: root + 'delete'
    }
}